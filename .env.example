# Playwright Testing URL
# URL base para ejecutar tests end-to-end con Playwright
# Si no se establece, se usa http://localhost:3000 por defecto
PLAYWRIGHT_BASE_URL=http://localhost:3000

# Node Environment (automático)
# Establecido automáticamente por Next.js: development, production, or test
# NODE_ENV=development
